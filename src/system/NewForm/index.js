/**
 * Internal dependencies
 */

import { FormSelect } from './FormSelect';
import { Form } from './Form';

const Select = FormSelect;
const Root = Form;

export { Root, Select };

export default Root;
