import React from "react";

import { action } from "@storybook/addon-actions";
import { Avatar } from "..";

export default {
	title: "Avatar",
	component: Avatar,
};

export const Default = () => <Avatar />;
